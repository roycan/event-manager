

  <html>
  <head>
    <meta charset="utf-8">
    <title> Thank You! </title>
  </head>
  <body>
    <h1> Thanks <%= name.capitalize %> </h1>
    <p> Thanks for coming to our conference. It wouldn't have been the same without you! </p>

    <p>
      Political activism is at the heart of any democracy and your voice needs to be heard.
      Please consider reaching out to your representatives:
    </p>

    <table>
      <% if legislators.kind_of? (Array) %>
        <tr> <th> Legislators </th> <th> Website </th> </tr>

        <% legislators.each do |legislator|%>
          <tr>
            <td> <%= legislator.name %> </td>
            <td> <%= legislator.urls.join(", ") unless  legislator.urls == nil%> </td>
          </tr>
        <%end %>

      <% else %>
        <tr> <th>  </th> </tr>
        <tr> <td> <%= legislators %> </td> </tr>
      <% end %>

    </table>
  </body>
  </html>
